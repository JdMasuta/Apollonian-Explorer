"""Core mathematics for Apollonian gasket generation.

- ``core.engine``: the exact inversive-coordinate engine (current).
- ``core.descartes`` / ``core.exact_math`` / ``core.gasket_generator``:
  the legacy hybrid-arithmetic pipeline, retained as a cross-check oracle
  until Milestone 2 retires it from production paths
  (see REVAMP_BLUEPRINT.md).
"""
